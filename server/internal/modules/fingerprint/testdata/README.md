# Fingerprint Test Data

`full/fingerprinthub_web.json` 是唯一完整语料，来自 `manifest.json` 锁定的
0x727/FingerprintHub 提交的 `web_fingerprint_v4.json`。其 SHA-256、规则数和导入
统计均由完整语料回归测试验证。`small/` 包含 aggregate 的成功、重复规则和失败校验
用例。所有文件仅供测试使用，不是运行时种子。
