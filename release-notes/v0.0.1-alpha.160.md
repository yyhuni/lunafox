## English

### Fixes

- Fixed confirmed purge so `./uninstall.sh --purge --confirm` removes the persisted `compose.override.yaml` after verified deployment cleanup.
- Kept the reversible uninstall path intact, preserving upgrade state, named volumes, `.env`, and the release directory unless an explicit purge is confirmed.
- Added regression coverage for override preservation, reset, late creation, and removal failures.

Full Changelog: https://github.com/yyhuni/lunafox/compare/v0.0.1-alpha.159...v0.0.1-alpha.160

## 简体中文

### 修复

- 修复确认清理流程：`./uninstall.sh --purge --confirm` 在验证部署资源完成删除后，会移除持久化的 `compose.override.yaml`。
- 保持普通卸载的可恢复行为：除非显式确认 purge，否则继续保留升级状态、命名 volumes、`.env` 和发布目录。
- 增加对 override 保留、重置、清理期间晚出现以及删除失败场景的回归覆盖。

完整变更记录：https://github.com/yyhuni/lunafox/compare/v0.0.1-alpha.159...v0.0.1-alpha.160
