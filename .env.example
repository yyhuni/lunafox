# SPDX-License-Identifier: GPL-3.0-only
# GENERATED / READ-ONLY deployment template.
# Edit .env in the extracted versioned deployment package before starting.
# Use single quotes for passwords containing $, # or spaces. Never commit .env.
PUBLIC_HOST=localhost
PUBLIC_PORT=443
PUBLIC_URL=https://localhost
# Required: choose independent, long random secrets for this installation.
DB_PASSWORD=
JWT_SECRET=
DB_USER=postgres
DB_NAME=lunafox
