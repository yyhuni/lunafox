import { LoginVisualSettingsWorkspace } from "@/components/settings/login-visual/login-visual-settings-workspace"

export default function LoginVisualPage() {
  return <LoginVisualSettingsWorkspace />
}
