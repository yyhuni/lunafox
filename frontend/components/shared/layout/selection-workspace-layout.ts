// Organization and target scope filters occupy the same toolbar slot, regardless
// of the translated option or the neighboring search input's available width.
export const SELECTION_WORKSPACE_SCOPE_TRIGGER_CLASS = "w-32 shrink-0"
