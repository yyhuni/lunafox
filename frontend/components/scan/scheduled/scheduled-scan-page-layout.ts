import { TABLE_DENSE_ROW_RHYTHM_HEIGHT_PX } from "@/components/ui/table"

export const SCHEDULED_SCAN_PAGE_SIZE = 10
// Narrow layouts keep the readable vertical sequence; wide layouts fit five
// nodes in one row, so the shared loading/resolved reservation can be shorter.
export const SCHEDULED_SCAN_TIMELINE_BODY_CLASS = "min-h-80 xl:min-h-40"
export const SCHEDULED_SCAN_TABLE_LOADING_ROW_HEIGHT_PX = TABLE_DENSE_ROW_RHYTHM_HEIGHT_PX
