metadata:
  # name missing
  version: "1.0.0"
  stages: []

tools:
  subfinder:
    metadata:
      display_name: "Subfinder"
      stage: "recon"
    base_command: "subfinder -d {{.Domain}}"
    cli_params: []
