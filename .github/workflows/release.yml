name: Release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-global
  cancel-in-progress: true

permissions:
  contents: write
  packages: write

env:
  RELEASE_CHANNEL_BRANCH: release-channel
  DOCKERHUB_NAMESPACE: ${{ vars.DOCKERHUB_NAMESPACE != '' && vars.DOCKERHUB_NAMESPACE || 'yyhuni' }}
  TCR_REGISTRY: ${{ vars.TCR_REGISTRY != '' && vars.TCR_REGISTRY || 'ccr.ccs.tencentyun.com' }}
  TCR_NAMESPACE: ${{ vars.TCR_NAMESPACE != '' && vars.TCR_NAMESPACE || 'yyhuni' }}
  ACR_REGISTRY: ${{ vars.ACR_REGISTRY }}
  ACR_NAMESPACE: ${{ vars.ACR_NAMESPACE != '' && vars.ACR_NAMESPACE || 'yyhuni' }}
  RELEASE_STAGING_TAG: ci-${{ github.run_id }}-${{ github.run_attempt }}

jobs:
  validate-tag:
    runs-on: ubuntu-22.04
    outputs:
      tag: ${{ steps.validate.outputs.tag }}
      channel: ${{ steps.validate.outputs.channel }}
      prerelease: ${{ steps.validate.outputs.prerelease }}
    steps:
      - id: validate
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME}"
          CHANNEL=""
          PRERELEASE="false"

          if [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            CHANNEL="stable"
          elif [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-(rc|beta|alpha)\.[0-9]+$ ]]; then
            CHANNEL="canary"
            PRERELEASE="true"
          else
            echo "Tag must be vX.Y.Z or vX.Y.Z-(rc|beta|alpha).N, got: $TAG" >&2
            exit 1
          fi

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "channel=$CHANNEL" >> "$GITHUB_OUTPUT"
          echo "prerelease=$PRERELEASE" >> "$GITHUB_OUTPUT"

  tests-gate:
    needs: validate-tag
    uses: ./.github/workflows/test.yml

  build-installers:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate]
    defaults:
      run:
        shell: bash
    env:
      GOMODCACHE: ${{ github.workspace }}/.cache/go/pkg/mod
      GOCACHE: ${{ github.workspace }}/.cache/go-build
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: tools/installer/go.mod

      - name: Prepare writable cache directories
        run: |
          set -euo pipefail
          mkdir -p "${GOMODCACHE}" "${GOCACHE}"

      - name: Cache Go modules and build cache
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.GOMODCACHE }}
            ${{ env.GOCACHE }}
          key: ${{ runner.os }}-go-cache-installer-${{ hashFiles('tools/installer/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-cache-installer-

      - name: Build installer binaries
        working-directory: tools/installer
        run: |
          set -euo pipefail
          TAG="${{ needs.validate-tag.outputs.tag }}"
          BUILD_TIME="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          OUT_DIR="$GITHUB_WORKSPACE/dist/release"
          mkdir -p "$OUT_DIR"

          build_one() {
            local goos="$1"
            local goarch="$2"
            local out="$3"
            CGO_ENABLED=0 GOOS="$goos" GOARCH="$goarch" \
              go build -trimpath \
                -buildvcs=false \
                -ldflags "-s -w \
                -X 'github.com/yyhuni/lunafox/tools/installer/internal/buildinfo.Version=${TAG}' \
                -X 'github.com/yyhuni/lunafox/tools/installer/internal/buildinfo.Commit=${GITHUB_SHA}' \
                -X 'github.com/yyhuni/lunafox/tools/installer/internal/buildinfo.BuildTime=${BUILD_TIME}'" \
                -o "$out" \
                ./cmd/lunafox-installer
            chmod +x "$out"
          }

          build_one "linux" "amd64" "$OUT_DIR/lunafox-installer-linux-amd64"
          build_one "linux" "arm64" "$OUT_DIR/lunafox-installer-linux-arm64"
          build_one "darwin" "amd64" "$OUT_DIR/lunafox-installer-darwin-amd64"
          build_one "darwin" "arm64" "$OUT_DIR/lunafox-installer-darwin-arm64"

          (cd "$OUT_DIR" && sha256sum lunafox-installer-* > checksums.txt)
          ls -lah "$OUT_DIR"

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-installers
          path: dist/release/*
          if-no-files-found: error

  prepare-github-release:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate, build-installers]
    steps:
      - name: Download installer artifact
        uses: actions/download-artifact@v4
        with:
          name: release-installers
          path: dist/release

      - name: Prepare GitHub draft release (phase 1)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.validate-tag.outputs.tag }}
          name: ${{ needs.validate-tag.outputs.tag }}
          draft: true
          prerelease: ${{ needs.validate-tag.outputs.prerelease == 'true' }}
          generate_release_notes: true
          files: |
            dist/release/lunafox-installer-linux-amd64
            dist/release/lunafox-installer-linux-arm64
            dist/release/lunafox-installer-darwin-amd64
            dist/release/lunafox-installer-darwin-arm64
            dist/release/checksums.txt
          fail_on_unmatched_files: true
          overwrite_files: true

  prepare-gitee-release:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate]
    env:
      GITEE_OWNER: ${{ secrets.GITEE_OWNER }}
      GITEE_REPO: ${{ secrets.GITEE_REPO }}
      GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate gitee secrets
        run: |
          set -euo pipefail
          [ -n "${GITEE_OWNER:-}" ] || { echo "missing GITEE_OWNER"; exit 1; }
          [ -n "${GITEE_REPO:-}" ] || { echo "missing GITEE_REPO"; exit 1; }
          [ -n "${GITEE_TOKEN:-}" ] || { echo "missing GITEE_TOKEN"; exit 1; }

      - name: Push tag to gitee
        run: |
          set -euo pipefail
          TAG="${{ needs.validate-tag.outputs.tag }}"
          git remote add gitee "https://oauth2:${GITEE_TOKEN}@gitee.com/${GITEE_OWNER}/${GITEE_REPO}.git"
          local_oid="$(git rev-parse "refs/tags/${TAG}^{commit}")"
          remote_line="$(git ls-remote --tags gitee "refs/tags/${TAG}" || true)"
          remote_oid="$(printf '%s' "$remote_line" | awk 'NR==1{print $1}')"

          if [ -n "${remote_oid}" ]; then
            if [ "$remote_oid" = "$local_oid" ]; then
              echo "gitee tag ${TAG} already exists with same commit, skip push"
              exit 0
            fi
            echo "gitee tag ${TAG} already exists but points to different commit: remote=${remote_oid}, local=${local_oid}" >&2
            exit 1
          fi

          git push gitee "refs/tags/${TAG}:refs/tags/${TAG}"

  publish-images-amd64:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate]
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        include:
          - image: lunafox-server
            context: ./server
            dockerfile: ./server/Dockerfile
          - image: lunafox-frontend
            context: ./frontend
            dockerfile: ./frontend/Dockerfile
          - image: lunafox-nginx
            context: ./docker/nginx
            dockerfile: ./docker/nginx/Dockerfile
          - image: lunafox-agent
            context: ./agent
            dockerfile: ./agent/Dockerfile
          - image: lunafox-worker
            context: ./worker
            dockerfile: ./worker/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push staging image (amd64)
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64
          push: true
          provenance: false
          sbom: false
          build-args: |
            GOPROXY=https://proxy.golang.org,direct
          cache-from: |
            type=registry,ref=ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:buildcache-amd64
          cache-to: |
            type=registry,ref=ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:buildcache-amd64,mode=max
          tags: |
            docker.io/${{ env.DOCKERHUB_NAMESPACE }}/${{ matrix.image }}:${{ env.RELEASE_STAGING_TAG }}-amd64
            ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:${{ env.RELEASE_STAGING_TAG }}-amd64

  publish-images-arm64:
    runs-on: ubuntu-22.04-arm
    needs: [validate-tag, tests-gate]
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        include:
          - image: lunafox-server
            context: ./server
            dockerfile: ./server/Dockerfile
          - image: lunafox-frontend
            context: ./frontend
            dockerfile: ./frontend/Dockerfile
          - image: lunafox-nginx
            context: ./docker/nginx
            dockerfile: ./docker/nginx/Dockerfile
          - image: lunafox-agent
            context: ./agent
            dockerfile: ./agent/Dockerfile
          - image: lunafox-worker
            context: ./worker
            dockerfile: ./worker/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push staging image (arm64)
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: linux/arm64
          push: true
          provenance: false
          sbom: false
          build-args: |
            GOPROXY=https://proxy.golang.org,direct
          cache-from: |
            type=registry,ref=ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:buildcache-arm64
          cache-to: |
            type=registry,ref=ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:buildcache-arm64,mode=max
          tags: |
            docker.io/${{ env.DOCKERHUB_NAMESPACE }}/${{ matrix.image }}:${{ env.RELEASE_STAGING_TAG }}-arm64
            ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:${{ env.RELEASE_STAGING_TAG }}-arm64

  merge-staging-images:
    runs-on: ubuntu-22.04
    needs:
      - validate-tag
      - tests-gate
      - publish-images-amd64
      - publish-images-arm64
    if: >
      always() &&
      needs.validate-tag.result == 'success' &&

      needs.tests-gate.result == 'success' &&
      needs.publish-images-amd64.result == 'success' &&
      needs.publish-images-arm64.result == 'success'
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Validate TCR and ACR credentials
        run: |
          set -euo pipefail
          [ -n "${TCR_REGISTRY:-}" ] || { echo "missing TCR_REGISTRY"; exit 1; }
          [ -n "${ACR_REGISTRY:-}" ] || { echo "missing ACR_REGISTRY"; exit 1; }
          [ -n "${{ secrets.TCR_USERNAME }}" ] || { echo "missing TCR_USERNAME"; exit 1; }
          [ -n "${{ secrets.TCR_PASSWORD }}" ] || { echo "missing TCR_PASSWORD"; exit 1; }
          [ -n "${{ secrets.ACR_USERNAME }}" ] || { echo "missing ACR_USERNAME"; exit 1; }
          [ -n "${{ secrets.ACR_PASSWORD }}" ] || { echo "missing ACR_PASSWORD"; exit 1; }

      - name: Log in to Tencent TCR (retry)
        run: |
          set -euo pipefail
          for delay in 0 2 4 8 12; do
            if [ "$delay" -gt 0 ]; then
              sleep "$delay"
            fi
            if printf '%s' "${{ secrets.TCR_PASSWORD }}" | docker login "${TCR_REGISTRY}" --username "${{ secrets.TCR_USERNAME }}" --password-stdin; then
              exit 0
            fi
          done
          echo "failed to log in to ${TCR_REGISTRY} after retries" >&2
          exit 1

      - name: Log in to Alibaba ACR (retry)
        run: |
          set -euo pipefail
          for delay in 0 2 4 8 12; do
            if [ "$delay" -gt 0 ]; then
              sleep "$delay"
            fi
            if printf '%s' "${{ secrets.ACR_PASSWORD }}" | docker login "${ACR_REGISTRY}" --username "${{ secrets.ACR_USERNAME }}" --password-stdin >/dev/null 2>&1; then
              exit 0
            fi
          done
          echo "failed to log in to ${ACR_REGISTRY} after retries" >&2
          exit 1

      - name: Merge staging manifests and persist digests
        run: |
          set -euo pipefail
          start_epoch="$(date +%s)"
          mkdir -p dist/image-digests

          retry_imagetools_inspect() {
            local ref="$1"
            local delays=(0 3 6 12 20 30)
            local delay
            for delay in "${delays[@]}"; do
              if [ "$delay" -gt 0 ]; then
                sleep "$delay"
              fi
              if docker buildx imagetools inspect "$ref" >/dev/null 2>&1; then
                return 0
              fi
            done
            echo "failed to inspect image ref after retries: $ref" >&2
            return 1
          }

          retry_imagetools_create() {
            local target="$1"
            shift
            local delays=(0 2 4)
            local delay
            for delay in "${delays[@]}"; do
              if [ "$delay" -gt 0 ]; then
                sleep "$delay"
              fi
              if docker buildx imagetools create --tag "$target" "$@" >/dev/null 2>&1; then
                return 0
              fi
            done
            echo "failed to create manifest after retries: $target" >&2
            return 1
          }

          resolve_digest() {
            local ref="$1"
            local delays=(0 2 4 8)
            local delay
            local digest
            for delay in "${delays[@]}"; do
              if [ "$delay" -gt 0 ]; then
                sleep "$delay"
              fi
              digest="$(docker buildx imagetools inspect "$ref" | awk '/Digest:/ {print $2; exit}')"
              if [[ "$digest" =~ ^sha256:[a-f0-9]{64}$ ]]; then
                printf '%s\n' "$digest"
                return 0
              fi
            done
            return 1
          }

          images=(
            lunafox-server
            lunafox-frontend
            lunafox-nginx
            lunafox-agent
            lunafox-worker
          )

          for image in "${images[@]}"; do
            docker_amd64_ref="docker.io/${DOCKERHUB_NAMESPACE}/${image}:${RELEASE_STAGING_TAG}-amd64"
            docker_arm64_ref="docker.io/${DOCKERHUB_NAMESPACE}/${image}:${RELEASE_STAGING_TAG}-arm64"
            ghcr_amd64_ref="ghcr.io/${GITHUB_REPOSITORY_OWNER}/${image}:${RELEASE_STAGING_TAG}-amd64"
            ghcr_arm64_ref="ghcr.io/${GITHUB_REPOSITORY_OWNER}/${image}:${RELEASE_STAGING_TAG}-arm64"

            retry_imagetools_inspect "$docker_amd64_ref"
            retry_imagetools_inspect "$docker_arm64_ref"
            retry_imagetools_inspect "$ghcr_amd64_ref"
            retry_imagetools_inspect "$ghcr_arm64_ref"

            docker_staging_ref="docker.io/${DOCKERHUB_NAMESPACE}/${image}:${RELEASE_STAGING_TAG}"
            ghcr_staging_ref="ghcr.io/${GITHUB_REPOSITORY_OWNER}/${image}:${RELEASE_STAGING_TAG}"
            tcr_staging_ref="${TCR_REGISTRY}/${TCR_NAMESPACE}/${image}:${RELEASE_STAGING_TAG}"
            acr_staging_ref="${ACR_REGISTRY}/${ACR_NAMESPACE}/${image}:${RELEASE_STAGING_TAG}"

            retry_imagetools_create "$docker_staging_ref" "$docker_amd64_ref" "$docker_arm64_ref"
            retry_imagetools_create "$ghcr_staging_ref" "$ghcr_amd64_ref" "$ghcr_arm64_ref"
            retry_imagetools_create "$tcr_staging_ref" "$ghcr_staging_ref"
            retry_imagetools_create "$acr_staging_ref" "$ghcr_staging_ref"

            # Wait for registry propagation (TCR/ACR need extra time)
            sleep 5

            retry_imagetools_inspect "$docker_staging_ref"
            retry_imagetools_inspect "$ghcr_staging_ref"
            retry_imagetools_inspect "$tcr_staging_ref"
            retry_imagetools_inspect "$acr_staging_ref"

            docker_digest="$(resolve_digest "$docker_staging_ref" || true)"
            ghcr_digest="$(resolve_digest "$ghcr_staging_ref" || true)"
            tcr_digest="$(resolve_digest "$tcr_staging_ref" || true)"
            acr_digest="$(resolve_digest "$acr_staging_ref" || true)"
            [[ "$docker_digest" =~ ^sha256:[a-f0-9]{64}$ ]] || { echo "invalid docker digest for ${image}: $docker_digest"; exit 1; }
            [[ "$ghcr_digest" =~ ^sha256:[a-f0-9]{64}$ ]] || { echo "invalid ghcr digest for ${image}: $ghcr_digest"; exit 1; }
            [[ "$tcr_digest" =~ ^sha256:[a-f0-9]{64}$ ]] || { echo "invalid tcr digest for ${image}: $tcr_digest"; exit 1; }
            [[ "$acr_digest" =~ ^sha256:[a-f0-9]{64}$ ]] || { echo "invalid acr digest for ${image}: $acr_digest"; exit 1; }

            {
              echo "IMAGE=${image}"
              echo "DIGEST=${docker_digest}"
              echo "STAGING_TAG=${RELEASE_STAGING_TAG}"
              echo "DOCKERHUB_REF=docker.io/${DOCKERHUB_NAMESPACE}/${image}@${docker_digest}"
              echo "GHCR_REF=ghcr.io/${GITHUB_REPOSITORY_OWNER}/${image}@${ghcr_digest}"
              echo "TCR_REF=${TCR_REGISTRY}/${TCR_NAMESPACE}/${image}@${tcr_digest}"
              echo "ACR_REF=${ACR_REGISTRY}/${ACR_NAMESPACE}/${image}@${acr_digest}"
            } > "dist/image-digests/${image}.env"
          done

          end_epoch="$(date +%s)"
          echo "merge_staging_images_duration_seconds=$((end_epoch - start_epoch))"

      - name: Upload image digest artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-image-digests
          path: dist/image-digests/*.env
          if-no-files-found: error

  update-release-channel:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate, build-installers, promote-images, finalize-github-release, finalize-gitee-release]
    if: >
      always() &&
      needs.validate-tag.result == 'success' &&

      needs.tests-gate.result == 'success' &&
      needs.build-installers.result == 'success' &&
      needs.promote-images.result == 'success' &&
      needs.finalize-github-release.result == 'success' &&
      needs.finalize-gitee-release.result == 'success'
    env:
      GITEE_OWNER: ${{ secrets.GITEE_OWNER }}
      GITEE_REPO: ${{ secrets.GITEE_REPO }}
      GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Download installer artifact
        uses: actions/download-artifact@v4
        with:
          name: release-installers
          path: dist/release

      - name: Download image digest artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-image-digests
          path: dist/image-digests

      - name: Generate channel files
        run: |
          set -euo pipefail
          TAG="${{ needs.validate-tag.outputs.tag }}"
          CHANNEL="${{ needs.validate-tag.outputs.channel }}"
          CHANNEL_DIR="$GITHUB_WORKSPACE/dist/channel"
          rm -rf "$CHANNEL_DIR"
          mkdir -p "$CHANNEL_DIR/channels"

          CHECKSUM_FILE="dist/release/checksums.txt"
          linux_amd64_sha="$(awk '$2=="lunafox-installer-linux-amd64"{print $1}' "$CHECKSUM_FILE")"
          linux_arm64_sha="$(awk '$2=="lunafox-installer-linux-arm64"{print $1}' "$CHECKSUM_FILE")"
          darwin_amd64_sha="$(awk '$2=="lunafox-installer-darwin-amd64"{print $1}' "$CHECKSUM_FILE")"
          darwin_arm64_sha="$(awk '$2=="lunafox-installer-darwin-arm64"{print $1}' "$CHECKSUM_FILE")"

          [ -n "$linux_amd64_sha" ] || { echo "missing linux amd64 checksum"; exit 1; }
          [ -n "$linux_arm64_sha" ] || { echo "missing linux arm64 checksum"; exit 1; }
          [ -n "$darwin_amd64_sha" ] || { echo "missing darwin amd64 checksum"; exit 1; }
          [ -n "$darwin_arm64_sha" ] || { echo "missing darwin arm64 checksum"; exit 1; }

          agent_dockerhub_ref="$(awk -F= '$1=="DOCKERHUB_REF"{print $2}' dist/image-digests/lunafox-agent.env | tr -d '\r')"
          agent_ghcr_ref="$(awk -F= '$1=="GHCR_REF"{print $2}' dist/image-digests/lunafox-agent.env | tr -d '\r')"
          agent_tcr_ref="$(awk -F= '$1=="TCR_REF"{print $2}' dist/image-digests/lunafox-agent.env | tr -d '\r')"
          agent_acr_ref="$(awk -F= '$1=="ACR_REF"{print $2}' dist/image-digests/lunafox-agent.env | tr -d '\r')"
          worker_dockerhub_ref="$(awk -F= '$1=="DOCKERHUB_REF"{print $2}' dist/image-digests/lunafox-worker.env | tr -d '\r')"
          worker_ghcr_ref="$(awk -F= '$1=="GHCR_REF"{print $2}' dist/image-digests/lunafox-worker.env | tr -d '\r')"
          worker_tcr_ref="$(awk -F= '$1=="TCR_REF"{print $2}' dist/image-digests/lunafox-worker.env | tr -d '\r')"
          worker_acr_ref="$(awk -F= '$1=="ACR_REF"{print $2}' dist/image-digests/lunafox-worker.env | tr -d '\r')"

          for ref in \
            "$agent_dockerhub_ref" "$agent_ghcr_ref" "$agent_tcr_ref" "$agent_acr_ref" \
            "$worker_dockerhub_ref" "$worker_ghcr_ref" "$worker_tcr_ref" "$worker_acr_ref"; do
            [[ "$ref" =~ ^.+@sha256:[a-f0-9]{64}$ ]] || { echo "invalid digest ref: $ref"; exit 1; }
          done

          cat > "$CHANNEL_DIR/channels/${TAG}.env" <<EOF
          SCHEMA_VERSION=2
          VERSION=${TAG}
          GITHUB_BASE_URL=https://github.com/${GITHUB_REPOSITORY}/releases/download/${TAG}
          GITEE_BASE_URL=https://gitee.com/${GITEE_OWNER}/${GITEE_REPO}/releases/download/${TAG}
          LINUX_AMD64_ASSET=lunafox-installer-linux-amd64
          LINUX_AMD64_SHA256=${linux_amd64_sha}
          LINUX_ARM64_ASSET=lunafox-installer-linux-arm64
          LINUX_ARM64_SHA256=${linux_arm64_sha}
          DARWIN_AMD64_ASSET=lunafox-installer-darwin-amd64
          DARWIN_AMD64_SHA256=${darwin_amd64_sha}
          DARWIN_ARM64_ASSET=lunafox-installer-darwin-arm64
          DARWIN_ARM64_SHA256=${darwin_arm64_sha}
          IMAGE_REGISTRY=docker.io
          IMAGE_NAMESPACE=${DOCKERHUB_NAMESPACE}
          AGENT_IMAGE_REFS=${agent_dockerhub_ref},${agent_ghcr_ref},${agent_tcr_ref},${agent_acr_ref}
          WORKER_IMAGE_REFS=${worker_dockerhub_ref},${worker_ghcr_ref},${worker_tcr_ref},${worker_acr_ref}
          EOF

          if [ "$CHANNEL" = "stable" ]; then
            cp "$CHANNEL_DIR/channels/${TAG}.env" "$CHANNEL_DIR/channels/stable.env"
          elif [ "$CHANNEL" = "canary" ]; then
            cp "$CHANNEL_DIR/channels/${TAG}.env" "$CHANNEL_DIR/channels/canary.env"
          else
            echo "unexpected channel: $CHANNEL" >&2
            exit 1
          fi

          if [ "$CHANNEL" = "stable" ]; then
            ./scripts/installer/verify-channel-schema.sh "$CHANNEL_DIR/channels/${TAG}.env" "$CHANNEL_DIR/channels/stable.env"
          else
            ./scripts/installer/verify-channel-schema.sh "$CHANNEL_DIR/channels/${TAG}.env" "$CHANNEL_DIR/channels/canary.env"
          fi

          echo "CHANNEL_DIR=$CHANNEL_DIR" >> "$GITHUB_ENV"

      - name: Publish release-channel to github and gitee
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          [ -n "${GITEE_OWNER:-}" ] || { echo "missing GITEE_OWNER"; exit 1; }
          [ -n "${GITEE_REPO:-}" ] || { echo "missing GITEE_REPO"; exit 1; }
          [ -n "${GITEE_TOKEN:-}" ] || { echo "missing GITEE_TOKEN"; exit 1; }

          WORK_DIR="$(mktemp -d)"
          trap 'rm -rf "$WORK_DIR"' EXIT
          REPO_URL="https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"

          if git clone --depth 1 --branch "${RELEASE_CHANNEL_BRANCH}" "$REPO_URL" "$WORK_DIR"; then
            :
          else
            git clone --depth 1 "$REPO_URL" "$WORK_DIR"
            (
              cd "$WORK_DIR"
              git checkout --orphan "${RELEASE_CHANNEL_BRANCH}"
              git rm -rf . >/dev/null 2>&1 || true
            )
          fi

          mkdir -p "$WORK_DIR/channels"
          cp "$CHANNEL_DIR/channels/"*.env "$WORK_DIR/channels/"

          (
            cd "$WORK_DIR"
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add channels/*.env
            if git diff --staged --quiet; then
              echo "release-channel unchanged"
            else
              git commit -m "chore(channel): update ${{ needs.validate-tag.outputs.channel }} to ${{ needs.validate-tag.outputs.tag }}"
              git push origin "${RELEASE_CHANNEL_BRANCH}"
            fi
            git remote add gitee "https://oauth2:${GITEE_TOKEN}@gitee.com/${GITEE_OWNER}/${GITEE_REPO}.git"
            git push gitee "${RELEASE_CHANNEL_BRANCH}:${RELEASE_CHANNEL_BRANCH}"
          )

  promote-images:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate, merge-staging-images]
    if: >
      always() &&
      needs.validate-tag.result == 'success' &&

      needs.tests-gate.result == 'success' &&
      needs.merge-staging-images.result == 'success'
    steps:
      - name: Download image digest artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-image-digests
          path: dist/image-digests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Validate TCR and ACR credentials
        run: |
          set -euo pipefail
          [ -n "${TCR_REGISTRY:-}" ] || { echo "missing TCR_REGISTRY"; exit 1; }
          [ -n "${ACR_REGISTRY:-}" ] || { echo "missing ACR_REGISTRY"; exit 1; }
          [ -n "${{ secrets.TCR_USERNAME }}" ] || { echo "missing TCR_USERNAME"; exit 1; }
          [ -n "${{ secrets.TCR_PASSWORD }}" ] || { echo "missing TCR_PASSWORD"; exit 1; }
          [ -n "${{ secrets.ACR_USERNAME }}" ] || { echo "missing ACR_USERNAME"; exit 1; }
          [ -n "${{ secrets.ACR_PASSWORD }}" ] || { echo "missing ACR_PASSWORD"; exit 1; }

      - name: Log in to Tencent TCR (retry)
        run: |
          set -euo pipefail
          for delay in 0 2 4 8 12; do
            if [ "$delay" -gt 0 ]; then
              sleep "$delay"
            fi
            if printf '%s' "${{ secrets.TCR_PASSWORD }}" | docker login "${TCR_REGISTRY}" --username "${{ secrets.TCR_USERNAME }}" --password-stdin >/dev/null 2>&1; then
              exit 0
            fi
          done
          echo "failed to log in to ${TCR_REGISTRY} after retries" >&2
          exit 1

      - name: Log in to Alibaba ACR (retry)
        run: |
          set -euo pipefail
          for delay in 0 2 4 8 12; do
            if [ "$delay" -gt 0 ]; then
              sleep "$delay"
            fi
            if printf '%s' "${{ secrets.ACR_PASSWORD }}" | docker login "${ACR_REGISTRY}" --username "${{ secrets.ACR_USERNAME }}" --password-stdin >/dev/null 2>&1; then
              exit 0
            fi
          done
          echo "failed to log in to ${ACR_REGISTRY} after retries" >&2
          exit 1

      - name: Promote staging digest to release tag (phase 2)
        run: |
          set -euo pipefail
          start_epoch="$(date +%s)"
          FINAL_TAG="${{ needs.validate-tag.outputs.tag }}"
          [ -n "$FINAL_TAG" ] || { echo "missing final tag"; exit 1; }
          PLAN_FILE="$(mktemp)"
          trap 'rm -f "$PLAN_FILE"' EXIT

          retry_imagetools_inspect() {
            local ref="$1"
            local delays=(0 2 4 8)
            local delay
            for delay in "${delays[@]}"; do
              if [ "$delay" -gt 0 ]; then
                sleep "$delay"
              fi
              if docker buildx imagetools inspect "$ref" >/dev/null 2>&1; then
                return 0
              fi
            done
            echo "failed to inspect image ref after retries: $ref" >&2
            return 1
          }

          retry_imagetools_create() {
            local target="$1"
            local src="$2"
            local delays=(0 2 4)
            local delay
            for delay in "${delays[@]}"; do
              if [ "$delay" -gt 0 ]; then
                sleep "$delay"
              fi
              if docker buildx imagetools create --tag "$target" "$src" >/dev/null 2>&1; then
                return 0
              fi
            done
            echo "failed to create manifest after retries: $target <- $src" >&2
            return 1
          }

          shopt -s nullglob
          files=(dist/image-digests/*.env)
          [ "${#files[@]}" -gt 0 ] || { echo "missing image digest artifacts"; exit 1; }

          # Preflight all source references first; only promote when all checks pass.
          for file in "${files[@]}"; do
            IMAGE=""
            DIGEST=""
            DOCKERHUB_REF=""
            GHCR_REF=""
            TCR_REF=""
            ACR_REF=""
            # shellcheck disable=SC1090
            source "$file"

            [ -n "$IMAGE" ] || { echo "missing IMAGE in $file"; exit 1; }
            [[ "$DIGEST" =~ ^sha256:[a-f0-9]{64}$ ]] || { echo "invalid DIGEST in $file: $DIGEST"; exit 1; }
            [ -n "$DOCKERHUB_REF" ] || { echo "missing DOCKERHUB_REF in $file"; exit 1; }
            [ -n "$GHCR_REF" ] || { echo "missing GHCR_REF in $file"; exit 1; }
            [ -n "$TCR_REF" ] || { echo "missing TCR_REF in $file"; exit 1; }
            [ -n "$ACR_REF" ] || { echo "missing ACR_REF in $file"; exit 1; }

            retry_imagetools_inspect "$DOCKERHUB_REF"
            retry_imagetools_inspect "$GHCR_REF"
            retry_imagetools_inspect "$TCR_REF"
            retry_imagetools_inspect "$ACR_REF"

            printf '%s|%s|%s|%s|%s|%s\n' "$IMAGE" "$DIGEST" "$DOCKERHUB_REF" "$GHCR_REF" "$TCR_REF" "$ACR_REF" >> "$PLAN_FILE"
          done

          while IFS='|' read -r IMAGE DIGEST DOCKERHUB_REF GHCR_REF TCR_REF ACR_REF; do
            docker_target="docker.io/${DOCKERHUB_NAMESPACE}/${IMAGE}:${FINAL_TAG}"
            ghcr_target="ghcr.io/${GITHUB_REPOSITORY_OWNER}/${IMAGE}:${FINAL_TAG}"
            tcr_target="${TCR_REGISTRY}/${TCR_NAMESPACE}/${IMAGE}:${FINAL_TAG}"
            acr_target="${ACR_REGISTRY}/${ACR_NAMESPACE}/${IMAGE}:${FINAL_TAG}"

            echo "promote ${IMAGE}: ${DIGEST}"
            retry_imagetools_create "$docker_target" "$DOCKERHUB_REF"
            retry_imagetools_create "$ghcr_target" "$GHCR_REF"
            retry_imagetools_create "$tcr_target" "$TCR_REF"
            retry_imagetools_create "$acr_target" "$ACR_REF"
            retry_imagetools_inspect "$docker_target"
            retry_imagetools_inspect "$ghcr_target"
            retry_imagetools_inspect "$tcr_target"
            retry_imagetools_inspect "$acr_target"
          done < "$PLAN_FILE"

          end_epoch="$(date +%s)"
          echo "promote_images_duration_seconds=$((end_epoch - start_epoch))"

  verify-tcr-images:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate, merge-staging-images, promote-images]
    if: >
      always() &&
      needs.validate-tag.result == 'success' &&

      needs.tests-gate.result == 'success' &&
      needs.merge-staging-images.result == 'success' &&
      needs.promote-images.result == 'success'
    steps:
      - name: Download image digest artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-image-digests
          path: dist/image-digests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate Tencent TCR credentials
        run: |
          set -euo pipefail
          [ -n "${TCR_REGISTRY:-}" ] || { echo "missing TCR_REGISTRY"; exit 1; }
          [ -n "${{ secrets.TCR_USERNAME }}" ] || { echo "missing TCR_USERNAME"; exit 1; }
          [ -n "${{ secrets.TCR_PASSWORD }}" ] || { echo "missing TCR_PASSWORD"; exit 1; }

      - name: Log in to Tencent TCR (retry)
        run: |
          set -euo pipefail
          for delay in 0 2 4 8 12; do
            if [ "$delay" -gt 0 ]; then
              sleep "$delay"
            fi
            if printf '%s' "${{ secrets.TCR_PASSWORD }}" | docker login "${TCR_REGISTRY}" --username "${{ secrets.TCR_USERNAME }}" --password-stdin >/dev/null 2>&1; then
              exit 0
            fi
          done
          echo "failed to log in to ${TCR_REGISTRY} after retries" >&2
          exit 1

      - name: Verify final image tags in Tencent TCR
        run: |
          set -euo pipefail
          start_epoch="$(date +%s)"

          retry_imagetools_inspect() {
            local ref="$1"
            local delays=(0 2 4 8)
            local delay
            for delay in "${delays[@]}"; do
              if [ "$delay" -gt 0 ]; then
                sleep "$delay"
              fi
              if docker buildx imagetools inspect "$ref" >/dev/null 2>&1; then
                return 0
              fi
            done
            echo "failed to inspect image ref after retries: $ref" >&2
            return 1
          }

          FINAL_TAG="${{ needs.validate-tag.outputs.tag }}"
          [ -n "$FINAL_TAG" ] || { echo "missing final tag"; exit 1; }

          for IMAGE_EXPECTED in lunafox-server lunafox-frontend lunafox-nginx lunafox-agent lunafox-worker; do
            FILE="dist/image-digests/${IMAGE_EXPECTED}.env"
            [ -f "$FILE" ] || { echo "missing digest file: $FILE"; exit 1; }

            IMAGE=""
            TCR_REF=""
            # shellcheck disable=SC1090
            source "$FILE"

            [ "$IMAGE" = "$IMAGE_EXPECTED" ] || { echo "image mismatch in $FILE: $IMAGE"; exit 1; }
            [ -n "$TCR_REF" ] || { echo "missing TCR_REF in $FILE"; exit 1; }

            tcr_target="${TCR_REGISTRY}/${TCR_NAMESPACE}/${IMAGE}:${FINAL_TAG}"
            retry_imagetools_inspect "$TCR_REF"
            retry_imagetools_inspect "$tcr_target"
          done

          end_epoch="$(date +%s)"
          echo "verify_tcr_images_duration_seconds=$((end_epoch - start_epoch))"

  verify-acr-images:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate, merge-staging-images, promote-images]
    if: >
      always() &&
      needs.validate-tag.result == 'success' &&

      needs.tests-gate.result == 'success' &&
      needs.merge-staging-images.result == 'success' &&
      needs.promote-images.result == 'success'
    steps:
      - name: Download image digest artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-image-digests
          path: dist/image-digests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate Alibaba ACR credentials
        run: |
          set -euo pipefail
          [ -n "${ACR_REGISTRY:-}" ] || { echo "missing ACR_REGISTRY"; exit 1; }
          [ -n "${{ secrets.ACR_USERNAME }}" ] || { echo "missing ACR_USERNAME"; exit 1; }
          [ -n "${{ secrets.ACR_PASSWORD }}" ] || { echo "missing ACR_PASSWORD"; exit 1; }

      - name: Log in to Alibaba ACR (retry)
        run: |
          set -euo pipefail
          for delay in 0 2 4 8 12; do
            if [ "$delay" -gt 0 ]; then
              sleep "$delay"
            fi
            if printf '%s' "${{ secrets.ACR_PASSWORD }}" | docker login "${ACR_REGISTRY}" --username "${{ secrets.ACR_USERNAME }}" --password-stdin >/dev/null 2>&1; then
              exit 0
            fi
          done
          echo "failed to log in to ${ACR_REGISTRY} after retries" >&2
          exit 1

      - name: Verify final image tags in Alibaba ACR
        run: |
          set -euo pipefail
          start_epoch="$(date +%s)"

          retry_imagetools_inspect() {
            local ref="$1"
            local delays=(0 2 4 8)
            local delay
            for delay in "${delays[@]}"; do
              if [ "$delay" -gt 0 ]; then
                sleep "$delay"
              fi
              if docker buildx imagetools inspect "$ref" >/dev/null 2>&1; then
                return 0
              fi
            done
            echo "failed to inspect image ref after retries: $ref" >&2
            return 1
          }

          FINAL_TAG="${{ needs.validate-tag.outputs.tag }}"
          [ -n "$FINAL_TAG" ] || { echo "missing final tag"; exit 1; }

          for IMAGE_EXPECTED in lunafox-server lunafox-frontend lunafox-nginx lunafox-agent lunafox-worker; do
            FILE="dist/image-digests/${IMAGE_EXPECTED}.env"
            [ -f "$FILE" ] || { echo "missing digest file: $FILE"; exit 1; }

            IMAGE=""
            ACR_REF=""
            # shellcheck disable=SC1090
            source "$FILE"

            [ "$IMAGE" = "$IMAGE_EXPECTED" ] || { echo "image mismatch in $FILE: $IMAGE"; exit 1; }
            [ -n "$ACR_REF" ] || { echo "missing ACR_REF in $FILE"; exit 1; }

            acr_target="${ACR_REGISTRY}/${ACR_NAMESPACE}/${IMAGE}:${FINAL_TAG}"
            retry_imagetools_inspect "$ACR_REF"
            retry_imagetools_inspect "$acr_target"
          done

          end_epoch="$(date +%s)"
          echo "verify_acr_images_duration_seconds=$((end_epoch - start_epoch))"

  finalize-github-release:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate, prepare-github-release, promote-images, verify-tcr-images, verify-acr-images]
    if: always()
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Verify release prerequisites
        run: |
          set -euo pipefail
          FAILED=()
          check_gate() {
            local name="$1"
            local result="$2"
            if [ "$result" != "success" ]; then
              FAILED+=("${name}=${result}")
            fi
          }

          check_gate "validate-tag" "${{ needs.validate-tag.result }}"
          check_gate "tests-gate" "${{ needs.tests-gate.result }}"
          check_gate "prepare-github-release" "${{ needs.prepare-github-release.result }}"
          check_gate "promote-images" "${{ needs.promote-images.result }}"
          check_gate "verify-tcr-images" "${{ needs.verify-tcr-images.result }}"
          check_gate "verify-acr-images" "${{ needs.verify-acr-images.result }}"

          if [ "${#FAILED[@]}" -gt 0 ]; then
            echo "github release blocked by prerequisite jobs:" >&2
            for item in "${FAILED[@]}"; do
              echo "  - ${item}" >&2
            done
            exit 1
          fi

      - name: Publish GitHub draft release (phase 2)
        run: |
          set -euo pipefail
          TAG="${{ needs.validate-tag.outputs.tag }}"
          PRERELEASE="${{ needs.validate-tag.outputs.prerelease }}"
          API="https://api.github.com/repos/${GITHUB_REPOSITORY}"
          TMP_DIR="$(mktemp -d)"
          trap 'rm -rf "$TMP_DIR"' EXIT

          extract_release_meta() {
            python3 - "$1" "$2" <<'PY'
          import json
          import sys

          path = sys.argv[1]
          tag = sys.argv[2]
          try:
              with open(path, "r", encoding="utf-8") as f:
                  payload = json.load(f)
          except Exception:
              print("")
              sys.exit(0)

          def emit(item):
              if not isinstance(item, dict):
                  return False
              release_id = item.get("id")
              if release_id is None:
                  return False
              draft = item.get("draft")
              print(f"{release_id} {'true' if draft else 'false'}")
              return True

          if isinstance(payload, dict):
              emit(payload)
              sys.exit(0)

          if isinstance(payload, list):
              for item in payload:
                  if isinstance(item, dict) and item.get("tag_name") == tag:
                      emit(item)
                      sys.exit(0)

          print("")
          PY
          }

          RELEASE_ID=""
          IS_DRAFT="false"
          LIST_STATUS=""

          STATUS=$(curl -sS -o "$TMP_DIR/release.json" -w "%{http_code}" \
            -H "Authorization: Bearer ${GITHUB_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            "${API}/releases/tags/${TAG}")

          if [ "$STATUS" -ge 200 ] && [ "$STATUS" -lt 300 ]; then
            read -r RELEASE_ID IS_DRAFT < <(extract_release_meta "$TMP_DIR/release.json" "$TAG")
          elif [ "$STATUS" -eq 404 ]; then
            LIST_STATUS=$(curl -sS -o "$TMP_DIR/releases.json" -w "%{http_code}" \
              -H "Authorization: Bearer ${GITHUB_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              "${API}/releases?per_page=100")

            if [ "$LIST_STATUS" -ge 200 ] && [ "$LIST_STATUS" -lt 300 ]; then
              read -r RELEASE_ID IS_DRAFT < <(extract_release_meta "$TMP_DIR/releases.json" "$TAG")
            else
              echo "failed to list github releases for fallback lookup, status=${LIST_STATUS}" >&2
              cat "$TMP_DIR/releases.json" >&2 || true
              exit 1
            fi
          else
            echo "failed to load github release for tag ${TAG}, status=${STATUS}" >&2
            cat "$TMP_DIR/release.json" >&2 || true
            exit 1
          fi

          if [ -z "${RELEASE_ID:-}" ]; then
            echo "missing github release id for tag ${TAG}" >&2
            echo "lookup by tag status: ${STATUS}" >&2
            if [ -n "$LIST_STATUS" ]; then
              echo "lookup by list status: ${LIST_STATUS}" >&2
            fi
            exit 1
          fi
          [ -n "${IS_DRAFT:-}" ] || IS_DRAFT="false"

          if [ "$IS_DRAFT" != "true" ]; then
            echo "github release for ${TAG} is already published"
            exit 0
          fi

          DETAIL_STATUS=$(curl -sS -o "$TMP_DIR/release-detail.json" -w "%{http_code}" \
            -H "Authorization: Bearer ${GITHUB_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            "${API}/releases/${RELEASE_ID}")
          if [ "$DETAIL_STATUS" -lt 200 ] || [ "$DETAIL_STATUS" -ge 300 ]; then
            echo "failed to load github release detail ${RELEASE_ID}, status=${DETAIL_STATUS}" >&2
            cat "$TMP_DIR/release-detail.json" >&2 || true
            exit 1
          fi

          PAYLOAD=$(python3 - "$TMP_DIR/release-detail.json" "$TAG" "$PRERELEASE" <<'PY'
          import json
          import re
          import sys

          path = sys.argv[1]
          tag = sys.argv[2]
          prerelease = sys.argv[3] == "true"

          with open(path, "r", encoding="utf-8") as f:
            payload = json.load(f)

          body = payload.get("body", "") if isinstance(payload, dict) else ""
          if body is None:
            body = ""
          body = str(body)

          marker_start = "<!-- codex-release-notice:start -->"
          marker_end = "<!-- codex-release-notice:end -->"
          pattern = re.compile(re.escape(marker_start) + r".*?" + re.escape(marker_end) + r"\n*", re.DOTALL)
          body = re.sub(pattern, "", body).lstrip()

          if prerelease:
            notice_lines = [
              marker_start,
              "## Release Notice",
              "- [CN] 预发布测试版本，仅用于验证和试用。",
              "- [EN] Pre-release build for validation and testing.",
              "- [CN] 不建议在生产环境使用。",
              "- [EN] Not recommended for production environments.",
              marker_end,
            ]
          else:
            notice_lines = [
              marker_start,
              "## Release Notice",
              "- [CN] 稳定版本，可用于生产环境。",
              "- [EN] Stable release intended for production use.",
              "- [CN] 升级前请先备份数据并阅读升级说明。",
              "- [EN] Review upgrade notes and back up data before upgrading.",
              marker_end,
            ]

          notice = "\n".join(notice_lines)
          merged_body = notice if not body else f"{notice}\n\n{body}"

          out = {
            "draft": False,
            "prerelease": prerelease,
            "name": tag,
            "body": merged_body,
          }
          print(json.dumps(out, ensure_ascii=False))
          PY
          )

          PATCH_STATUS=$(curl -sS -o "$TMP_DIR/patch.json" -w "%{http_code}" \
            -X PATCH "${API}/releases/${RELEASE_ID}" \
            -H "Authorization: Bearer ${GITHUB_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD")

          if [ "$PATCH_STATUS" -lt 200 ] || [ "$PATCH_STATUS" -ge 300 ]; then
            echo "failed to publish github release ${RELEASE_ID}, status=${PATCH_STATUS}" >&2
            cat "$TMP_DIR/patch.json" >&2 || true
            exit 1
          fi

  finalize-gitee-release:
    runs-on: ubuntu-22.04
    needs: [validate-tag, tests-gate, build-installers, prepare-gitee-release, promote-images, verify-tcr-images, verify-acr-images]
    if: always()
    env:
      GITEE_OWNER: ${{ secrets.GITEE_OWNER }}
      GITEE_REPO: ${{ secrets.GITEE_REPO }}
      GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
    steps:
      - name: Verify release prerequisites
        run: |
          set -euo pipefail
          FAILED=()
          check_gate() {
            local name="$1"
            local result="$2"
            if [ "$result" != "success" ]; then
              FAILED+=("${name}=${result}")
            fi
          }

          check_gate "validate-tag" "${{ needs.validate-tag.result }}"
          check_gate "tests-gate" "${{ needs.tests-gate.result }}"
          check_gate "build-installers" "${{ needs.build-installers.result }}"
          check_gate "prepare-gitee-release" "${{ needs.prepare-gitee-release.result }}"
          check_gate "promote-images" "${{ needs.promote-images.result }}"
          check_gate "verify-tcr-images" "${{ needs.verify-tcr-images.result }}"
          check_gate "verify-acr-images" "${{ needs.verify-acr-images.result }}"

          if [ "${#FAILED[@]}" -gt 0 ]; then
            echo "gitee release blocked by prerequisite jobs:" >&2
            for item in "${FAILED[@]}"; do
              echo "  - ${item}" >&2
            done
            exit 1
          fi

      - name: Download installer artifact
        uses: actions/download-artifact@v4
        with:
          name: release-installers
          path: dist/release

      - name: Validate gitee secrets
        run: |
          set -euo pipefail
          [ -n "${GITEE_OWNER:-}" ] || { echo "missing GITEE_OWNER"; exit 1; }
          [ -n "${GITEE_REPO:-}" ] || { echo "missing GITEE_REPO"; exit 1; }
          [ -n "${GITEE_TOKEN:-}" ] || { echo "missing GITEE_TOKEN"; exit 1; }

      - name: Publish Gitee release (phase 2)
        run: |
          set -euo pipefail
          # Gitee Releases API does not expose a draft field, so create
          # release only after image promotion completes.
          TAG="${{ needs.validate-tag.outputs.tag }}"
          PRERELEASE="${{ needs.validate-tag.outputs.prerelease }}"
          TARGET_COMMITISH="${GITHUB_SHA}"
          [ -n "$TARGET_COMMITISH" ] || { echo "missing target commitish"; exit 1; }
          API="https://gitee.com/api/v5/repos/${GITEE_OWNER}/${GITEE_REPO}"
          TMP_DIR="$(mktemp -d)"
          trap 'rm -rf "$TMP_DIR"' EXIT

          if [ "$PRERELEASE" = "true" ]; then
            RELEASE_NOTICE="[CN] 预发布测试版本，仅用于验证和试用；不建议在生产环境使用。 [EN] Pre-release build for validation and testing; not recommended for production environments."
          else
            RELEASE_NOTICE="[CN] 稳定版本，可用于生产环境；升级前请先备份数据并阅读升级说明。 [EN] Stable release intended for production use; review upgrade notes and back up data before upgrading."
          fi
          RELEASE_BODY="${RELEASE_NOTICE} [EN] Automated release for ${TAG}."

          PAYLOAD=$(python3 - "$TAG" "$TARGET_COMMITISH" "$PRERELEASE" "$RELEASE_BODY" <<'PY'
          import json
          import sys

          tag = sys.argv[1]
          target_commitish = sys.argv[2]
          prerelease = sys.argv[3] == "true"
          body = sys.argv[4]

          payload = {
            "tag_name": tag,
            "target_commitish": target_commitish,
            "name": tag,
            "body": body,
            "prerelease": prerelease,
          }
          print(json.dumps(payload, ensure_ascii=False))
          PY
          )

          extract_release_id() {
            python3 - "$1" <<'PY'
          import json
          import sys

          path = sys.argv[1]
          try:
              with open(path, "r", encoding="utf-8") as f:
                  payload = json.load(f)
          except Exception:
              print("")
              sys.exit(0)

          if isinstance(payload, dict):
              release_id = payload.get("id")
              if release_id is not None:
                  print(release_id)
                  sys.exit(0)

          print("")
          PY
          }

          STATUS=$(curl -sS -o "$TMP_DIR/create.json" -w "%{http_code}" \
            -X POST "${API}/releases?access_token=${GITEE_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD")

          RELEASE_ID=""
          LOOKUP_STATUS=""
          COMPAT_STATUS=""

          if [ "$STATUS" -ge 200 ] && [ "$STATUS" -lt 300 ]; then
            RELEASE_ID="$(extract_release_id "$TMP_DIR/create.json")"
          fi

          # Compatibility fallback: some gitee instances may reject or ignore
          # prerelease fields. Retry create with a minimal payload.
          if [ -z "$RELEASE_ID" ] && [ "$PRERELEASE" = "true" ]; then
            COMPAT_PAYLOAD=$(python3 - "$TAG" "$TARGET_COMMITISH" "$RELEASE_BODY" <<'PY'
          import json
          import sys

          tag = sys.argv[1]
          target_commitish = sys.argv[2]
          body = sys.argv[3]

          payload = {
            "tag_name": tag,
            "target_commitish": target_commitish,
            "name": tag,
            "body": body,
          }
          print(json.dumps(payload, ensure_ascii=False))
          PY
            )
            COMPAT_STATUS=$(curl -sS -o "$TMP_DIR/create-compat.json" -w "%{http_code}" \
              -X POST "${API}/releases?access_token=${GITEE_TOKEN}" \
              -H "Content-Type: application/json" \
              -d "$COMPAT_PAYLOAD")
            if [ "$COMPAT_STATUS" -ge 200 ] && [ "$COMPAT_STATUS" -lt 300 ]; then
              RELEASE_ID="$(extract_release_id "$TMP_DIR/create-compat.json")"
            fi
          fi

          if [ -z "$RELEASE_ID" ]; then
            for delay in 0 2 4 6; do
              if [ "$delay" -gt 0 ]; then
                sleep "$delay"
              fi
              LOOKUP_STATUS=$(curl -sS -o "$TMP_DIR/release.json" -w "%{http_code}" \
                "${API}/releases/tags/${TAG}?access_token=${GITEE_TOKEN}")
              if [ "$LOOKUP_STATUS" -ge 200 ] && [ "$LOOKUP_STATUS" -lt 300 ]; then
                RELEASE_ID="$(extract_release_id "$TMP_DIR/release.json")"
                if [ -n "$RELEASE_ID" ]; then
                  break
                fi
              fi
            done
          fi

          if [ -z "$RELEASE_ID" ]; then
            echo "failed to resolve gitee release id for tag ${TAG}" >&2
            echo "create status: ${STATUS}" >&2
            if [ -n "${COMPAT_STATUS}" ]; then
              echo "compat create status: ${COMPAT_STATUS}" >&2
            fi
            if [ -n "${LOOKUP_STATUS}" ]; then
              echo "lookup status: ${LOOKUP_STATUS}" >&2
            fi
            echo "create response:" >&2
            cat "$TMP_DIR/create.json" >&2 || true
            if [ -f "$TMP_DIR/create-compat.json" ]; then
              echo "compat create response:" >&2
              cat "$TMP_DIR/create-compat.json" >&2 || true
            fi
            if [ -f "$TMP_DIR/release.json" ]; then
              echo "lookup response:" >&2
              cat "$TMP_DIR/release.json" >&2 || true
            fi
            exit 1
          fi

          for file in dist/release/lunafox-installer-* dist/release/checksums.txt; do
            name="$(basename "$file")"
            curl -fsSL \
              -X POST "${API}/releases/${RELEASE_ID}/attach_files?access_token=${GITEE_TOKEN}" \
              -F "name=${name}" \
              -F "file=@${file}" >/dev/null
          done
