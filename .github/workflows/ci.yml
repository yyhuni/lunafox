name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

permissions:
  contents: read

jobs:
  check-generated:
    uses: ./.github/workflows/check-generated-files.yml
  
  test:
    uses: ./.github/workflows/test.yml
